# Vercel Deployment Tracker

## Steps
- [x] 1. Code fixes (route.js paths, /analyze endpoint, frontend → public)
- [ ] 2. Backend deps: cd backend && npm install
- [ ] 3. Install Vercel CLI: npm i -g vercel
- [ ] 4. vercel login
- [ ] 5. vercel deploy
- [ ] 6. Add env vars (user: GROQ_API_KEY, DATABASE_URL, REDIS_URL)
- [ ] 7. Prisma migrate: cd backend && npx prisma db push
- [ ] 8. Test live

**Current: Step 1 [x] complete. Waiting vercel login, then deploy.**
