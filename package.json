{
  "name": "arvyax-journal-system",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "dev-local": "cd backend && node server.js",
    "build": "cd backend && prisma generate",
    "vercel-build": "cd backend && prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",

    "dotenv": "^16.4.5",

    "pg": "^8.13.0",
    "prisma": "^5.20.0"
  }
}
